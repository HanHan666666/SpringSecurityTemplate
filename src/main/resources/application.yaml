server:
  port: 12342
#  servlet:
#    context-path: /springboot2
#  数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://bt6s.onbed.cn:3366/neuedu?useUnicode=true&useSSL=true&characterEncoding=utf8
    username: root
    password: yourpassword
  output:
    ansi:
      enabled: always
  security:
    user:
      name: admin
      password: 123
  redis:
    port: 3379
    host: 120.48.8.219
    password: '000415'
    database: 1

logging:
  level:
    org.springframework: INFO
mybatis-plus:
  mapper-locations: classpath:h/ssm/mapper/*.xml
  type-aliases-package: h.ssm.entity
  configuration:
    log-impl: org.apache.ibatis.logging.commons.JakartaCommonsLoggingImpl
token:
  jwt:
    secret: YWFhYWFhYWFhYQ==
    expire: 604800
    header: token
